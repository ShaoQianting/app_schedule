<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_today">

    <fragment
        android:id="@+id/nav_today"
        android:name="com.example.schedule.ui.today.TodayFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_today" />

    <fragment
        android:id="@+id/nav_importance"
        android:name="com.example.schedule.ui.importance.ImportanceFragment"
        android:label="@string/menu_importance"
        tools:layout="@layout/fragment_importance" />

    <fragment
        android:id="@+id/nav_tasks"
        android:name="com.example.schedule.ui.tasks.TasksFragment"
        android:label="@string/menu_tasks"
        tools:layout="@layout/fragment_tasks" />
</navigation>